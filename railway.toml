[build]
builder = "nixpacks"
buildCommand = "pnpm install --frozen-lockfile && pnpm run db:generate && pnpm --filter @atomquest/api build"

[deploy]
startCommand = "pnpm --filter @atomquest/api start"
healthcheckPath = "/api/v1"
healthcheckTimeout = 100
